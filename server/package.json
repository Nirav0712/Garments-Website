{
  "name": "server",
  "version": "1.0.0",
  "description": "Product List Backend API Server",
  "main": "src/index.js",
  "scripts": {
    "build": "node scripts/set-db-provider.js --force-prod && prisma generate",
    "build:prod": "node scripts/set-db-provider.js --force-prod && prisma generate",
    "prestart": "node scripts/set-db-provider.js && prisma generate",
    "start": "node src/index.js",
    "predev": "node scripts/set-db-provider.js && prisma generate",
    "dev": "nodemon src/index.js",
    "db:setup": "node scripts/set-db-provider.js && prisma generate",
    "db:push": "npm run db:setup && prisma db push",
    "db:seed": "node prisma/seed.js",
    "prisma:generate": "npm run db:setup && prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/adapter-pg": "^5.22.0",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.23.0",
    "slugify": "^1.6.6"
  },
  "devDependencies": {
    "nodemon": "^3.1.7",
    "prisma": "^5.22.0"
  }
}

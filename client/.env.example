# During local development, VITE_API_URL can be omitted because Vite proxies /api and /uploads to localhost:5000 automatically.
# In production, set this to your backend domain url.
VITE_API_URL=https://api.yourdomain.com/api
